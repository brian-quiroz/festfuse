[tool.ruff]
target-version = "py314"
line-length = 88
extend-exclude = ["migrations/versions"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.pytest.ini_options]
markers = [
    "postgres: requires a migrated local PostgreSQL database",
]
